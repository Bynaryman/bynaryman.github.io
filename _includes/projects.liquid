<div class="col mb-4">
  <article class="card h-100">
    {% if project.img -%}
      {%- assign project_image_alt = project.img_alt | default: project.title -%}
      {%- include figure.liquid path=project.img sizes="(min-width: 992px) 360px, (min-width: 576px) 45vw, 90vw" alt=project_image_alt class="card-img-top" -%}
    {%- endif %}
    <div class="card-body">
      <h3 class="card-title">
        <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">{{ project.title }}</a>
      </h3>
      <p class="card-text">{{ project.description }}</p>
      {% if project.github -%}
        <a href="{{ project.github }}" class="project-source"
          ><i class="fa-brands fa-github" aria-hidden="true"></i> Source code<span class="sr-only"> for {{ project.title }}</span></a
        >
      {%- endif %}
    </div>
  </article>
</div>
